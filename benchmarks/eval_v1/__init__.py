"""Authorization A — V1 evaluation-infrastructure smoke test.

This package exists to execute the 3-document smoke test defined in
`docs/evaluation/PROTOCOL_V1.md` §9.1 and Authorization A. It records
whether each protocol stage runs; it does not tune anything and does
not produce a benchmark claim.
"""
