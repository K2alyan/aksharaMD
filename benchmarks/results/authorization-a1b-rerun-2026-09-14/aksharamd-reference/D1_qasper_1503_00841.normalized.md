**Robustly Leveraging Prior Knowledge in Text Classification**

**Biao Liu** Tsinghua University Haidian District, Beijing, China liubiao2638@gmail.com

**Abstract**

Prior has been shown ful to address many natural language process- ing tasks. Many approaches have posed formalise variety however, whether the proposed approach is ro- bust or sensitive to the knowledge supplied to model has rarely been discussed. paper, propose three regularization on top of generalized expectation criteria, and extensive robustness proposed methods. imental results demonstrate that our proposed methods obtain remarkable improvements and are much more robust than baselines.

**Introduction** We posses a wealth of prior knowledge about many natural language processing tasks. in text categorization, know that *NBA, player, basketball* are strong indicators of

## arXiv:1503.00841v1 [cs.CL] 3 Mar 2015

*sports* category (Druck et al., like *terrible, boring messing tive* polarity while words like *perfect, moving* suggest a polarity in sentiment clas- sification. A key arisen here, such knowledge to guide the learning process, an in- teresting problem for both NLP and machine learn- ing communities. Previous studies fall into several lines. label data (Haghighi 2006; Allan, 2007). code **Minlie Huang** Tsinghua University Haidian District, Beijing, China aihuang@tsinghua.edu.cn

which can be commonly seen in many Bayesian ap- proaches (Andrzejewski and Zhu, 2009; Andrzejew- very use- ski et al., 2011). Third, formalise knowl- edge with additional variables and dependencies (Li been pro- et al., 2010). Last, to use prior knowledge to control knowledge, the distributions over latent output variables (Grac ̧a et al., 2007; McCallum et al., 2007; Chang et al., 2007), which makes output variables easily in- terpretable. However, crucial which has rarely justify been addressed, bias in Exper- that supply learning model. Would model be robust or sensitive to the prior knowledge? Or, which kind appropriate task? Let’s see an example: may *baseball* fan but unfamiliar *hockey* so that pro- vide a few number of feature words of *baseball* , but less *hockey baseball-hockey* classifi- cation task. Such prior knowledge may mislead the For example, model heavy bias *baseball* If model words such as cannot handle situation appropriately, per- formance may be undesirable. 2008), words indicate *nega-* paper, investigate into in *exciting* Generalized Expectation Criteria (McCallum et al., 2007). The study aims reveal factors reducing sensibility how leverage therefore make model more robust and practical. To this end, we introduce aux- iliary regularization terms in which our prior knowl- addressing edge formalized as over output vari- First, leverage ables. Recall the example just mentioned, though we Klein, do not have enough Second, en- *hockey* it easy us parameters, neutral words, namely words that are not strong in-

dicators of any class, like *player* factors revealed in this paper, supplying neutral fea- ture words boost performance making the model more robust. More attractively, we do not need manual annota- tion to label these neutral feature words posed approach. More specifically, explore tion terms to address the problem: tion associated maximum entropy tion term; (3) KL divergence erence and predicted class distribution. manner, we simply use the most common features as neutral features and assume the neutral features are distributed uniformly over class labels. ond and third one, we assume we have some knowl- edge about tailed soon later. To summarize, main work are as follows:

*•* explore three regularization model more robust: associated mum entropy tion term, and the KL divergence between ref- erence and predicted class distribution.

*•* Experiments demonstrate that the proposed ap- proaches outperform baselines and work much more robustly.

The rest paper structured Section 2, we briefly describe the generalized expec- tation criteria present tion terms. Section 3, periments to justify the proposed methods. vey related work in Section 4, work in Section 5.

**Method** address robustness FL (Druck et al., 2008), GE ages labeled features as prior knowledge. feature strong indicator is manually provided to the classifier. words like *amazing, exciting* can be labeled features for class in sentiment classification. here. As one of the **2.1 Generalized Expectation Criteria** Generalized expectation (GE) criteria (McCallum et remarkably, al., 2007) provides us a natural way to directly con- strain model ample, when in our pro- dataset the model to predict out a pre-specified class distri- three regulariza- bution. (1) a regulariza- Formally, features; (2) function, regulariza- value of some constraint functions about the model’s between ref- expectation. For the first conditional cal distribution function *S* , a GE term can be expressed as follows: For the sec-

which will de- **2.2 Learning from Labeled Features** contributions Druck et al. labeled features using generalized expectation crite- ria. When given a set of labeled features make erence regularization denoted features, maxi- divergence regulariza- the model predicted distribution of the objective function:

*O* = *k K* where *θ yi* as follows: importance proposed regulariza- extensive ex- *p* We sur- *θ* summarize

in which otherwise, of instances with a non-zero value of feature top GE- *p* ( *y* x) takes a softmax form as follows: *θ* method which lever- A labeled *p θ* specific For example, To solve be used for parameter estimation. in preferred direction. For ex- know proportion each in classification task, guide

in parameter estimation objective GE expresses preferences Given constraint function *G* (x *, y* ) model *p* ( *y* x) an empiri- *θ* ̃ *p* (x) over input samples and a score

*S* ( *E* [ *E* [ *G* (x *, y* )]]) (1) ̃ *p* (x) *p* ( *y* x) *θ*

(2008) proposed learn *K* , the ref- over classes these ˆ *p* ( *y* ) *, k K* introduces *k* between reference *p* ( *y* ) , as a term *θ k*

*θ* *<u>yi</u>* (2) *KL* (ˆ *p* ( *y* ) *|| p* ( *y* )) + *k θ k σ y,i*

model parameter which indicates word *y* The predicted *p* ( *y* ) can be expressed as follows: *θ k*

*p* ( *y* x) ( ) ( *y* ) = *k θ k k* ( ) if feature *k* occurs in instance *k* = <sup>( ) number</sup> *k* <sup>*k*</sup> *k* , and

*θ* ) ( *y* x) = <sup>exp(</sup> *yi Z* (x) optimization L-BFGS

GE, tained setting constraint <sup>(</sup> <sup>*y*</sup> <sup>) ( ) , where</sup> <sup>*⃗I*</sup> <sup>(</sup> <sup>*y*</sup> <sup>) is an indicator vector with</sup> <sup>*k*</sup> *k* *~~⃗~~* at index corresponding where.

**2.3 Regularization Terms** reduces heavy load tation performs well when knowledge with no bias. In our experiments, we ob- serve that comparable numbers for each class have to be supplied. before, it often case that provide enough knowledge for some of the classes. For the *baseball-hockey* classification task, as shown before, GE-FL will predict most of the instances as *baseball* section, will to make the model more robust.

**2.3.1 Regularization Associated with Neutral Features** Neutral mative indicator any classes, *player baseball-hockey* Such usually frequent categories. When set tion of the neutral features to be uniform distributed, these will prevent biasing that has labeled features. Formally, given set uniform ˆ *p* ( *y u* where number classes. function with the new term becomes

*O* = *O* + *KL* (ˆ *p* ( *y u NE* <sup>*′*</sup> *k K* Note that we do not need manual annotation to pro- vide neutral features. One simple way is to take the most common as mental results show that this strategy works success- fully.

**2.3.2 Regularization with Maximum Entropy Principle** Another way prevent desired direction dicted unlabeled ob- lacking knowledge about the class distribution of the function *G* (x *, y* ) = data, feasible way take maximum entropy principle, as below: label *y* else- *O* = *O* + *p* ( *y* ) log *p* ( *y* ) (4) *ME y*

where *p* ( *y* ) is the predicted class distribution, given instance anno- *p* ( *y* ) = <sup>*p*</sup> <sup>(</sup> <sup>*y*</sup> <sup>x) To control the influence</sup> <sup>*θ*</sup> of this term on the overall objective function, we can tune according to the difference in the number of labeled labeled features of each class. In this paper, we sim- But as mentioned ply set proportional total number not able labeled features, say = *β K* This maximum entropy derived setting constraint function *G* (x *, y* ) = *⃗I* ( *y* ) Therefore, *E* [ *G* (x *, y* )] is just the model distri- *p* ( *y* x) *θ* show three bution *p* ( *y* x) its expectation empiri- *θ* cal distribution ̃ *p* (x) is simply the average over input samples, namely *p* ( *y* ) When *S* takes the maximum entropy form, derive objective function as above. that not infor- instance, word **2.3.3 Regularization with KL Divergence** classification task. Sometimes, have already had knowl- words across all edge about corpus, estimate preference distribu- roughly without labeling instances. Therefore, we introduce the KL divergence between model predicted reference distributions into dominate number the objective function. Given preference ˆ *p* ( *y* ) *′ K* modify the objective function as follows: *′* <sup>*, k K*</sup> ) = *k* The objective *O* = *O* + *λKL* (ˆ *p* ( *y* ) *|| p* ( *y* )) (5) *KL*

Similarly, we set = *β K* ) *|| p* ( *y* )) (3) This divergence term can be derived by setting the *k θ k* constraint function to *G* (x *, y* ) = *⃗I* ( *y* ) and setting the <sup><u>ˆ</u></sup> <sup>*<u>p</u>*</sup> <sup>where</sup> <sup>*p*</sup> score function *S* (ˆ *p, p* ) = <sup>ˆ</sup> <sup>*p*</sup> <sup>log</sup> *p* ˆ *p* are distributions. Note that this regularization term involves the reference class distribution which features. Experi- will be discussed later.

**3 Experiments** section, first justify approach when there exists unbalance in the number of labeled fea- model drifting tures or in distribution. Then, test in- constrain pre- fluence data. When the method which incorporates the KL divergence of

class distribution. Last, we evaluate our approaches in 9 commonly used text classification datasets. set = 5 *K* default there is explicit declaration. The baseline we choose here is GE-FL (Druck et al., 2008), a method based on generalization expec- tation criteria.

**3.1 Data Preparation** We evaluate our methods on several commonly used datasets whose themes page, science medical bag-of-words feature preprocess stage. Though we have labels of all doc- uments, we do not use them during the learning pro- cess, instead, we use the label of features. The *movie* dataset, in which the task is to classify the movie reviews as testing the proposed approaches with unbalanced la- beled features, unbalanced datasets or different rameters All based *movie* dataset documents ment, we conduct 10-fold cross validation.

**Labeled Features** As described in (Druck et al., 2008), there are two ways obtain labeled use information gain. information of all features according to the labels of documents tures for each class as a feature pool. Note that using information gain requires this is only to simulate how we human provide prior model. LDA (Blei et al., 2003) to select features. We use the same selection process as (Druck et al., 2008), where they first train a LDA on the dataset, and then select most probable *P* ( *w* ) , the probability of word *j* Similar (Schapire 2008), estimate labeled using classes in total, and with a feature *k* , the probability that feature lated any

We also experimented on other datasets, and observed sim- ilar results. in all unless

range sentiment, web- healthcare. remove stopwords in or *negtive* , is used for

datasets constructed removing class. For each experi- features. The first way first calculate mutual

top 20 as labeled document label,

The second way

each (sorted *w* given topic et al., 2002; Druck et reference heuristic strategy. If there *n* classes are associated *k* is re- <sup>9</sup> *n* classes *n* any other class is *|− n* Neutral most frequent words af- ter removing stop words, their reference distri- butions are uniformly distributed. We use the top 10 frequent words as neutral features in all experiments.

**3.2 With Unbalanced Labeled Features**

In this section, we evaluate our approach when there categories classified. The labeled features are obtained through information gain. Two settings are chosen: use [1 20] feature pool class, only feature for the other. The original balanced *movie* dataset is used (positive:negative=1:1). **(b)** Similar (a), but dataset obtained by randomly removing 75% docu- ments (positive:negative=1:4). As shown in Figure 1, Maximum entropy princi- pa- ple shows improvement only balanced case. An obvious reason is that maximum entropy only fa- vors uniform distribution. Incorporating Neutral features performs similarly maximum entropy since assume that words uniformly distributed. Its accuracy de- creases slowly when the number of labeled features becomes larger ( *t >* 4 ) (Figure 1(a)), suggesting that the model gradually biases to the class with more la- beled features, just like GE-FL. fea- Incorporating the KL divergence of class distribu- tion performs much better than GE-FL on both bal- but anced and unbalanced datasets. This shows that it is effective to control the unbalance in labeled features use and in the dataset.

**3.3 With Balanced Labeled Features**

We also compare with the baseline when the labeled balanced. Similar experiment ). *j* above, the labeled features are obtained by informa- al., tion gain. Two settings are experimented with: [1 20] the feature pool for each class, and conduct compar- isons original balanced *movie* dataset (posi- tive:negtive=1:1).

Previous work shows the model is insensitive to the setting of the reference distribution.

balanced dataset (b) unbalanced dataset(1:4) Figure 1: Performance labeled features, tested *movie* dataset. Randomly feature pool (x-axis) labeled features for one class, and select feature for the other. The unbalanced datasets in (b) are constructed by randomly removing 75% of the documents.

balanced dataset (b) unbalanced dataset(1:4)

Figure 2:

Performance with balanced labeled features, tested on the *movie* dataset. Randomly select from the feature pool (x-axis) labeled features for each class. The unbalanced datasets in (b) are constructed by randomly removing 75% of the documents.

**(b)** Similar to (a), but the class distribution is un- tion misleads it. balanced, by randomly removing 75% doc- **3.4 With Unbalanced Class Distributions** uments (positive:negative=1:4).

Results are shown in Figure 2. When the dataset is Our methods are also evaluated on datasets with dif- balanced (Figure 2(a)), there little difference be- ferent distributions. manually tween methods. The reason that construct several *movie* datasets with class distribu- proposed regularization no addi- tions 1:2, 1:3, 1:4 removing 50%, tional knowledge to the model and there is no bias in 67%, 75% documents. The original bal- the labeled features. On the unbalanced dataset (Fig- anced *movie* dataset is used as a control group. ure 2(b)), incorporating KL divergence is much bet- test with both balanced and unbalanced labeled fea- tures. For the balanced case, we randomly select 10 ter than since additional knowl- edge(the true distribution), but maximum en- features from the feature pool for each class, and for case, tropy worse because class, and 1 feature for the other. Results are shown forcing the model to approach the uniform distribu-

![![Image on page 5](asset://6b38a2f9d0a4)]

![![Image on page 5](asset://d566efffbc48)]

![![Image on page 5](asset://ad16e6421e27)]

balanced labeled features

Figure 3:

Influence of unbalanced class distribution, tested on the each in (a), and, randomly removing 50%, 67%, 75% documents of the

balanced dataset Figure 4: The influence tested for one class, keep removing 75% documents of the

in Figure 3. Figure 3 shows that labeled both tle difference between our methods and GE-FL(also see Figure 2(a)). But when becomes more more remarkable. Performance maximum entropy decrease incorporating KL divergence This suggests if have more about distribution, KL the model to the right direction.

Figure 3 (b) shows that when the labeled features

(b) unbalanced labeled features *movie* dataset. We provide 10 labeled features for other in (b). The three datasets constructed class.

(b) unbalanced dataset(1:4) *movie* dataset. Randomly feature pool (x-axis) labeled feature other. The datasets in (b) constructed by class.

forms GE-FL. Incorporating KL divergence ro- bust enough to control unbalance both in the dataset when dataset and in labeled features while the other three methods balanced, there lit- are not so competitive.

**3.5 The Influence of** difference becomes present influence method that significantly but incorporates KL divergence in this section. Since we increases remarkably. simply set = *β K* , we just tune *β* here. Note that accurate when *β* = newly introduced regularization divergence guide disappeared, thus model actually GE-FL. Again, test method different in two settings: significantly outper- [1 20]

![![Image on page 6](asset://32c2687cb3c4)]

![![Image on page 6](asset://30c5e2556408)]

![![Image on page 6](asset://dcbb30822355)]

| Dataset | GE-FL | Neutral Features | Max Entropy | KL Divergence |
| --- | --- | --- | --- | --- |
| movie | 0.623 | 0.672 | 0.681 | 0.684 |
| sraa | 0.559 | 0.628 | 0.618 | 0.547 |
| webkb | 0.615 | 0.685 | 0.640 | 0.646 |
| med-space ibm-mac baseball-hockey 20 newsgroups | 0.927 0.817 0.915 0.667 | 0.921 0.796 0.935 0.669 | 0.936 0.837 0.925 0.680 | 0.928 0.833 0.923 0.677 |
| financial-healthcare sector.top | 0.588 0.596 | 0.618 0.653 | 0.618 0.581 | 0.507 0.639 |

Table 1:

Performance using LDA-features.

feature pool class, ture for the other class. The original balanced dataset is used (positive:negative=1:1). **(b)** Similar (a), but obtained by randomly removing 75% ments (positive:negative=1:4). Results are shown in Figure 4. flects how strong regularization tends closer preferences creasing of on both cases.

**3.6 Using LDA Selected Features** compare datasets in section. Instead obtained information gain, lect labeled features. Unlike information gain, LDA does not employ any instance features. setting, tion models without any instance annotation, but just with labeled features.

Table 1 shows that our three methods significantly

outperform GE-FL. Incorporating performs better than maximum entropy better divergence better on 7 datasets. LDA selects out most labeled without considering among classes. does on such an issue, so the performance is severely suf- fered. Our introduce tion control such promote the model significantly. Bold text means the method performs better than GE-FL.

only fea- **4 Related Work** *movie* There have been work that incorporate into learning, two related lines dataset surveyed here. One is to use prior knowledge to la- docu- bel unlabeled instances then apply standard learning algorithm. The other constrain As expected, re- model directly with prior knowledge. is. The model Liu et al.(2004) manually labeled which in- highly predictive unsupervised clustering as- signments use them label unlabeled data. Chang et al.(2007) proposed constraint driven learn- ing. They first used constraints learned model to annotate unlabeled instances, and then up- dated the model with the newly labeled data. Daum ́e all 9 (2008) proposed a self training method in which sev- using eral models are trained on the same dataset, and only use LDA se- unlabeled instances that satisfy the cross task knowl- edge constraints are used in the self training process. labels find labeled MaCallum et al.(2007) proposed generalized ex- build classifica- pectation(GE) criteria which formalised knowl- edge as constraint terms about the expectation of the model into the objective function.Grac ̧a et al.(2007) proposed posterior regularization(PR) which projects model’s posterior onto set 7 9 datasets, distributions that satisfy auxiliary constraints. 8 datasets, KL Druck et al.(2008) explored constraints labeled in GE forcing predictive as model’s predicted feature approach balance the reference distribution. Andrzejewski et al.(2011) not exert any control proposed in which general domain easily incorporated into LDA. Altendorf et al.(2012) explored monotonicity con- auxiliary regulariza- bias thus straints to improve the accuracy while learning from sparse data. Chen et al.(2013) tried to learn compre-

hensible models leveraging knowledge. Mann McCallum (2007; not only labeled but like into GE-FL. But they discussed supervised perspective did robustness unlike in this paper. There are also some active learning methods try- ing use knowledge. proposed use feedback interlacedly, and demonstrated that feedback on fea- tures boosts model much. proposed an active learning labels rather than used GE-FL to train the model.

**5 Conclusion and Discussions**

This paper investigates into leverage prior knowledge robustly in learning mod- els. propose three regularization of generalized expectation criteria. experimental results, be considerably improved when taking into account these factors. Comparative results show that our pro- posed methods is more effective and works more ro- bustly against baselines. To edge, this is the first work to address the robustness leveraging knowledge, other research. then present more detailed the three regularization methods. Incorporating neu- tral simplest which doesn’t require any modification but just finding out common Figure 1(a) shows, only using not strong enough handle labeled features. The maximum entropy regularization term shows the strong ability of controlling unbalance. This method doesn’t need and is thus suitable when we know nothing about the corpus. But this method assumes that the categories are uniformly distributed, which may not be the case in practice, and it will have a degraded performance if the assumption is violated (see Figure 1(b), Figure multi-domain 2(b), Figure 3(a)). The KL divergence performs better un- balanced corpora than other methods. The reason is 2010) incorporated that KL divergence utilizes the reference class distri- also other bution and doesn’t make any assumptions. The fact objective function suggests that additional knowledge does benefit the only semi- model. not investigate into However, KL divergence requires pro- what addressed viding true distribution. Sometimes, may have exact about true dis- tribution, but sometimes may not. Fortunately, et al.(2006) model insensitive true instances therefore rough estimation true distribu- tion is sufficient. In our experiments, when the true Druck et al.(2009) 1:2, where reference method which solicits distribution is set to 1:1.5/1:2/1:2.5, the accuracy is instances then 0.755/0.756/0.760 respectively. This provides us the possibility to perform simple computing on the cor- pus obtain in reality. Or, set the distribution roughly with domain expertise. how

top As demonstrated performance best knowl-

may inspire discussions about

way regularization, features. But as

extremely any extra knowledge,

**References** [Altendorf et al.2012] Eric E Altendorf, ficar, and Thomas G Dietterich. sparse data exploiting monotonicity *arXiv preprint arXiv:1207.1364* [Andrzejewski and Zhu2009] David Xiaojin Zhu. 2009. Latent topic-in-set knowledge. *Proceedings of the NAACL HLT 2009 Workshop on Semi-Supervised Learning for Natural Language Processing* tion for Computational Linguistics. [Andrzejewski et al.2011] David Zhu, Mark Craven, Benjamin incorporating edge into latent dirichlet allocation logic. *IJCAI Proceedings-International Joint Con- ference Artificial Intelligence* 1171. [Blei et al.2003] David Blei, Michael Jordan. 2003. Latent *Journal machine Learning* 1022. [Chang et al.2007] Ming-Wei Chang, Dan Roth. 2007. Guiding constraint-driven learning. *ASSOCIATION FOR COMPUTATIONAL TICS* , volume 45, page 280. Citeseer. [Chen et al.2013] Zhiyuan Chen, Liu, Meichun Hsu, Malu Castellanos, and Riddhiman Ghosh. 2013. Leveraging multi-domain prior knowl- edge in models. *Proceedings Third international joint conference on Artificial Intel- ligence* , pages 2071–2077. AAAI Press. [Daum ́e III2008] Hal Daum ́e III. knowledge-constrained self *conference empirical language processing* pages Computational Linguistics. [Druck et al.2008] Gregory Druck, Andrew McCallum. 2008. Learning from labeled fea- tures using generalized expectation *ceedings of the 31st annual international ACM SIGIR conference Research tion retrieval* , pages 595–602. ACM. [Druck et al.2009] Gregory Druck, drew McCallum. 2009. Active ing features. *Proceedings Empirical Methods in Natural ing: Volume 1-Volume 1* , pages 81–90. Association for Computational Linguistics. [Grac ̧a et al.2007] Joao Grac ̧a, Ben Taskar. 2007. Expectation maximization and pos- terior constraints. [Haghighi and Klein2006] Aria Haghighi and Dan Klein. 2006. Prototype-driven learning for sequence models. Angelo Resti- *Proceedings of the main conference on Human Lan-* 2012. Learning from *guage Technology Conference North American* constraints. *Chapter of the Association of Computational Linguis- tics* pages 320–327. Association Computational Andrzejewski Linguistics. dirichlet allocation [Li et al.2010] Fangtao Li, Minlie Huang, Xiaoyan Zhu. 2010. Sentiment analysis with global topics and local dependency. *AAAI* , pages 43–48. Associa- [Liu et al.2004] Bing Liu, Xiaoli Li, Wee Sun Lee, Philip S Yu. 2004. Text classification labeling Andrzejewski, Xiaojin words. *AAAI* , volume 4, pages 425–430. Recht. 2011. A [Mann and McCallum2007] Gideon S Mann and Andrew general domain knowl- McCallum. 2007. Simple, robust, scalable semi- using first-order supervised learning via expectation regularization. *Proceedings 24th international conference* volume 22, page *Machine learning* , pages 593–600. ACM. [Mann and McCallum2010] Gideon S Mann and Andrew Andrew Y Ng, McCallum. 2010. Generalized expectation criteria dirichlet allocation. for semi-supervised learning with weakly labeled data. *research* 3:993– *The Journal Machine Learning Research* 11:955– 984. Lev Ratinov, [McCallum et al.2007] Andrew McCallum, Gideon semi-supervision Mann, Gregory Druck. 2007. Generalized *ANNUAL MEETING-* expectation criteria. *Computer science technical note, LINGUIS- University of Massachusetts, Amherst, MA* [Raghavan and Allan2007] Hema James Arjun Mukherjee, Bing Allan. 2007. An interactive algorithm for asking and incorporating feature feedback into support vector ma- chines. *Proceedings 30th annual interna- Twenty- tional ACM SIGIR conference on Research and devel- opment in information retrieval* , pages 79–86. ACM. [Raghavan et al.2006] Hema Raghavan, Omid Madani, 2008. Cross-task Rosie Jones. 2006. Active learning feed- training. *Proceedings* back instances. *The Journal Ma- in natural chine Learning Research* , 7:1655–1686. 680–688. Association [Schapire et al.2002] Robert E Schapire, Marie Rochery, Mazin Rahim, Narendra Gupta. 2002. Incorpo- Gideon Mann, rating into boosting. *ICML* vol- ume 2, pages 538–545. criteria. *Pro-*

*development in informa-* Burr Settles, An- learning label- *2009 Conference Language Process-*

Kuzman Ganchev,